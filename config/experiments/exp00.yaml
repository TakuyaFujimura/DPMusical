# @package _global_
defaults:
    - override /datamodule: default
    - override /model: default

name: exp00

refresh_rate: 1

path:
  exp_root: logs

model:
  musical_path: null
  clean_path: null
  num_input: 1
  reduction: "median"